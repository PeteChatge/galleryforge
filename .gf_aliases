#!/usr/bin/env bash
# GalleryForge alias wrapper – kept for Termux compatibility.
# Canonical definitions live in shell/aliases.sh
_GF_ALIASES_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$_GF_ALIASES_DIR/shell/aliases.sh"
unset _GF_ALIASES_DIR
